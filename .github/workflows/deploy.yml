name: deploy

on:
  push:
    branches:
      - main
      - staging
  repository_dispatch:
  workflow_dispatch:

jobs:
  build:
    name: Build site
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }}
      url: ${{ github.ref == 'refs/heads/main' && 'https://open.ribose.com' || 'https://staging-open.ribose.com' }}

    steps:
      - uses: actions/checkout@v4

      - uses: unfor19/install-aws-cli-action@v1
      - uses: actions/setup-node@v4
        with:
          node-version: '16'
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Install OpenCC
        run: |
          sudo apt-get update
          sudo apt-get install -y opencc

      - name: Convert zh-hant to zh-hans
        run: |
          # Convert Traditional Chinese posts to Simplified Chinese
          # Only convert files that don't already exist in zh-hans/_posts
          # This allows manual overrides in zh-hans/_posts
          ruby scripts/convert_hant_to_hans.rb

      - name: Build site
        env:
          JEKYLL_ENV: production
        run: |
          make _site

      - name: Upload to AWS
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
          S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }}
        run: |
          aws s3 sync _site s3://$S3_BUCKET_NAME --region=$AWS_REGION --delete --no-progress --exclude "*" --include "*.html" --content-type "text/html; charset=utf-8"
          aws s3 sync _site s3://$S3_BUCKET_NAME --region=$AWS_REGION --delete --no-progress --exclude "*" --include "*.json" --content-type "text/json; charset=utf-8"
          aws s3 sync _site s3://$S3_BUCKET_NAME --region=$AWS_REGION --delete --no-progress --exclude "*.html,*.json" --include "*"
          aws cloudfront create-invalidation --distribution-id $CLOUDFRONT_DISTRIBUTION_ID --paths "/*"
